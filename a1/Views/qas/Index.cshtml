@model IEnumerable<a1.Models.qa>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Qa_title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Qa_body)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.user_t.Username)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Qa_title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Qa_body)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.user_t.Username)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Qa_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Qa_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Qa_id })
        </td>
    </tr>
}

</table>
